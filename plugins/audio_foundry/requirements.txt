# Audio Foundry — core service deps only.
# Per-backend model deps are added as each backend is wired, to keep
# `pip install` fast when the plugin is first enabled.

fastapi==0.115.0
uvicorn==0.30.0
PyYAML>=6.0
pydantic>=2.0
httpx>=0.27.0

# Testing
pytest>=8.0.0

# audio_fx (SAO) runtime deps — torch, diffusers, transformers, accelerate are
# already in the project's backend/venv; soundfile is added here.
soundfile>=0.12

# Future per-backend deps added when each backend lands:
#   voice_gen_chatterbox -> chatterbox-tts
#   voice_gen_kokoro     -> kokoro
#   music_gen_acestep    -> ace-step (or HF transformers + custom pipeline)
