# Audio Foundry — core service deps only.
# Per-backend model deps are added as each backend is wired, to keep
# `pip install` fast when the plugin is first enabled.

fastapi==0.115.0
uvicorn==0.30.0
PyYAML>=6.0
pydantic>=2.0
httpx>=0.27.0

# Testing
pytest>=8.0.0

# audio_fx (SAO) runtime deps — torch, diffusers, transformers, accelerate are
# already in the project's backend/venv; soundfile is added here.
soundfile>=0.12

# voice_gen runtime deps. Both are optional in the sense that the plugin
# starts and tests pass without them; first real call to /generate/voice
# will fail with a clear "pip install ..." message if either is missing.
chatterbox-tts>=0.1
kokoro>=0.7

# music_gen runtime deps. Same opt-in story — ACE-Step is heavy (~10 GB
# weights) so we don't pull it on every install. First /generate/music
# call surfaces a clear pip-install error if missing.
acestep>=0.1
