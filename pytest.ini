[pytest]
markers =
    smoke: live agent-display smoke tests that require DISPLAY=:99, Ollama, and app services
    db: database tests
    api: api tests
    indexing: indexing tests
    rules: rules tests
